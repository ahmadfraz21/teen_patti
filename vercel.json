{
  "installCommand": "curl -fsSL https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.19.0-stable.tar.xz | tar xJ -C $HOME && export PATH=$PATH:$HOME/flutter/bin && flutter pub get",
  "buildCommand": "export PATH=$PATH:$HOME/flutter/bin && cd frontend && flutter build web --release",
  "outputDirectory": "frontend/build/web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
